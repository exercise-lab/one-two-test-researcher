---
ssh_key_file: "/home/lupyanlab/.ssh/id_rsa"

keymap: /usr/local/etc/keymap

experiment: "one-two-test"
problem_repo: "git@github.com:exercise-lab/{{ experiment }}-{{ language }}-{{ problem }}.git"
solution_repo: "git@github.com:exercise-lab/{{ experiment }}-{{ language }}-{{ problem }}-{{ username }}.git"
problems_dir: "/home/{{ username }}/problems-{{ language }}/"
problem_dst: "{{ problems_dir }}/{{ problem }}"

keylog_dir: /home/lupyanlab/keylogs/
keylog_file: "{{ keylog_dir }}/{{ experiment }}-keys-{{ username }}.log"

browser_histories_dir: /home/lupyanlab/browser_histories/

scripts_dir: "/home/{{ username }}/.bin"
snapshot_script_dst: "{{ scripts_dir }}/snapshot-{{ problem }}-{{ language }}-repo"
